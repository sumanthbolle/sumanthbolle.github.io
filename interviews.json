[
  {
    "id": 16,
    "question": "Design a Script Include that implements the **Factory** and **Strategy** patterns to process incident priority calculations based on urgency and impact. Ensure it respects scoped application boundaries and can be safely called via GlideAjax from a client script on the incident form. Provide the complete code and explain how singleton scoping prevents multiple instantiations.[difficulty:Senior]",
    "answer": "<h4>Key Concept</h4><p>The **Factory** pattern creates objects without specifying the exact class, while the **Strategy** pattern defines a family of algorithms (priority calculators) interchangeable at runtime. In ServiceNow, Script Includes in scoped apps must use <strong>scoped GlideRecord</strong> and avoid global objects. Singleton ensures one instance per session. Client callable Script Includes extend AbstractAjaxProcessor for GlideAjax, with <strong>getXMLAnswer()</strong> returning XML/JSON.</p><h4>Code Example</h4><pre>var IncidentPriorityFactory = Class.create();\nIncidentPriorityFactory.prototype = {\n    initialize: function() {},\n    getCalculator: function(urgency, impact) {\n        var urgencyNum = parseInt(urgency);\n        var impactNum = parseInt(impact);\n        if (urgencyNum &gt;= 4 &amp;&amp; impactNum &gt;= 4) return new HighPriorityStrategy();\n        if (urgencyNum &gt;= 3 || impactNum &gt;= 3) return new MediumPriorityStrategy();\n        return new LowPriorityStrategy();\n    },\n    TYPE: function() { return 'IncidentPriorityFactory'; }\n};\n\nvar HighPriorityStrategy = Class.create();\nHighPriorityStrategy.prototype = {\n    calculate: function(record) {\n        var gr = new GlideRecord('incident');\n        gr.addQuery('sys_id', record.sys_id).query();\n        if (gr.next()) {\n            gr.priority = 1;\n            gr.update();\n        }\n    },\n    TYPE: function() { return 'HighPriorityStrategy'; }\n};</pre><p>This **Factory** (<strong>IncidentPriorityFactory</strong>) creates Strategy objects based on urgency/impact conditions with a query and loop. HighPriorityStrategy uses scoped GlideRecord on 'incident' table. For singleton, wrap in <strong>new Singleton(IncidentPriorityFactory)</strong> or check <code>if (!this.instance) this.instance = new IncidentPriorityFactory();</code>.</p><h4>GlideAjax Boundaries</h4><p>Make client-callable: Check 'Client callable', extend AbstractAjaxProcessor. In <strong>initialize()</strong>, parse urgency/impact from <code>this.getParameter('sysparm_urgency')</code>. Use factory: <code>var calc = factory.getCalculator(urgency, impact); calc.calculate(current);</code>. Return <code>'&lt;priority&gt;1&lt;/priority&gt;'</code> from getXMLAnswer(). Client: <code>var ga = new GlideAjax('IncidentPriorityFactory'); ga.addParam('sysparm_name', 'calculatePriority'); ga.addParam('sysparm_urgency', g_form.getValue('urgency')); ga.getXMLAnswer(callback);</code>.</p><p>Scoped access: Use <strong>gs.getUser().hasRole('itil')</strong> check. Avoid cross-scope with <strong>gs.crossScopeAccessSafe()</strong>. Patterns reduce duplication; Factory centralizes logic, Strategy swaps algorithms without if-else chains. Singleton caches instance: <code>getInstance: function() { return this.instance || (this.instance = new IncidentPriorityFactory()); }</code>. This ensures thread-safety in concurrent server calls.</p><p>Benefits: Reusable, testable (mock strategies), maintains single responsibility. In interviews, emphasize <strong>why patterns matter</strong> - scalability over procedural code.",
    "difficulty": "Senior",
    "company": "ServiceNow",
    "category": "Scripting",
    "date": "Dec 18, 2025",
    "dateISO": "2025-12-18T18:05:58.921Z"
  },
  {
    "id": 1,
    "question": "Explain the Factory design pattern in ServiceNow Script Includes and demonstrate how to use it with GlideAjax to dynamically invoke different API handlers based on a company identifier. Provide code examples for the Factory Script Include, specific API handlers, and a client-side GlideAjax call.",
    "answer": "<h4>Key Concept</h4>\n<p>The <strong>Factory pattern</strong> in ServiceNow lets you choose and instantiate different Script Include “handlers” at runtime based on input (here: company). A single client-callable Script Include acts as the factory and uses <strong>GlideScopedEvaluator</strong> to turn a dynamically chosen Script Include name into a real object, whose common <code>execute()</code> method is then called and returned to the client via <strong>GlideAjax</strong>.</p>\n\n<h4>Correct: Company-specific API handler Script Includes</h4>\n<p>Each handler has the same interface (<code>execute()</code>) but different logic.</p>\n<pre>\n// CompanyOneAPI (server-side Script Include)\nvar CompanyOneAPI = Class.create();\nCompanyOneAPI.prototype = {\n  initialize: function() {},\n  execute: function(companyId) {\n    var gr = new GlideRecord('incident');\n    gr.addQuery('company', companyId);\n    gr.setLimit(1);\n    gr.query();\n    if (gr.next())\n      return 'CompanyOne: first incident is ' + gr.number;\n    return 'CompanyOne: no incidents found';\n  },\n  type: 'CompanyOneAPI'\n};\n</pre>\n\n<pre>\n// CompanyTwoAPI (server-side Script Include)\nvar CompanyTwoAPI = Class.create();\nCompanyTwoAPI.prototype = {\n  initialize: function() {},\n  execute: function(companyId) {\n    var count = 0;\n    var gr = new GlideRecord('incident');\n    gr.addQuery('company', companyId);\n    gr.query();\n    while (gr.next())\n      count++;\n    return 'CompanyTwo: total incidents = ' + count;\n  },\n  type: 'CompanyTwoAPI'\n};\n</pre>\n\n<h4>Correct: Factory Script Include (client-callable + dynamic instantiation)</h4>\n<p>This is the “dummy” Script Include record used as a factory. It looks up the correct handler name by company, injects that name into its script field, and evaluates it into an object.</p>\n<pre>\n// ApiFactory (client callable)\nvar ApiFactory = Class.create();\nApiFactory.prototype = {\n  initialize: function() {},\n  \n  getResult: function() {\n    var companyId = gs.getSession().getProperty('sysparm_company') || \n                    gs.getProperty('sysparm_company');\n    var handlerName = this._getHandlerName(companyId); // e.g. 'CompanyOneAPI'\n    if (!handlerName)\n      return 'No handler configured for company';\n\n    var grFactory = new GlideRecord('sys_script_include');\n    grFactory.get('name', 'ApiFactory');          // factory placeholder\n    grFactory.script = handlerName;               // IMPORTANT: just the class name\n    var evaluator = new GlideScopedEvaluator();\n    var model = evaluator.evaluateScript(grFactory, 'script');\n    var handler = new model();\n    return handler.execute(companyId);\n  },\n\n  _getHandlerName: function(companyId) {\n    var map = new GlideRecord('u_company_api_map');\n    map.addQuery('u_company', companyId);\n    map.query();\n    if (map.next())\n      return map.u_script_include;               // e.g. 'CompanyOneAPI'\n    return '';\n  },\n\n  type: 'ApiFactory'\n};\n</pre>\n\n<h4>Incorrect Approach (what to avoid)</h4>\n<pre>\n// WRONG: hard-coding logic and not using a factory\nvar BadApi = Class.create();\nBadApi.prototype = {\n  execute: function(companyId) {\n    if (companyId == 'COMP1')\n      return new CompanyOneAPI().execute(companyId);\n    if (companyId == 'COMP2')\n      return new CompanyTwoAPI().execute(companyId);\n    return 'Unknown company';\n  },\n  type: 'BadApi'\n};\n</pre>\n<p>This is brittle: every new company requires code changes and deployments in this Script Include instead of just configuration.</p>\n\n<h4>Client-side GlideAjax usage</h4>\n<p>The client script calls the factory, passing the company, and receives the handler’s result.</p>\n<pre>\nfunction onChange(control, oldValue, newValue, isLoading) {\n  if (isLoading || !newValue)\n    return;\n\n  var ga = new GlideAjax('ApiFactory');\n  ga.addParam('sysparm_name', 'getResult');\n  ga.addParam('sysparm_company', newValue);\n  ga.getXMLAnswer(function(answer) {\n    g_form.setValue('u_result', answer);\n  });\n}\n</pre>\n\n<p>This complete example shows a correct Factory pattern in ServiceNow: multiple company-specific handlers with a common interface, a dynamic factory Script Include using GlideScopedEvaluator, and a client-side GlideAjax call that invokes the right handler based on the company identifier.</p>",
    "difficulty": "Senior",
    "company": "ServiceNow",
    "category": "Scripting",
    "date": "Dec 18, 2025"
  },
  {
    "id": 2,
    "question": "In a scenario where you need to get the total number of active incidents per assignment group for reporting purposes, explain why using GlideAggregate is generally more performant than using GlideRecord with getRowCount(), and show example code for both approaches. When, if ever, would GlideRecord be more appropriate in this use case?",
    "answer": "<h4>GlideRecord vs GlideAggregate for counting records</h4><p>GlideRecord loads each matching row into memory and then counts them, while GlideAggregate asks the database to perform the aggregation (COUNT, SUM, etc.) and returns only summarized results. For large tables, GlideAggregate is typically more performant and scalable because it reduces data transfer and server-side processing.</p><pre>// Less efficient: GlideRecord + getRowCount()\nvar gr = new GlideRecord('incident');\ngr.addActiveQuery();\ngr.addNotNullQuery('assignment_group');\ngr.query();\n// This iterates resultset on app server to count rows\nvar total = gr.getRowCount();\ngs.info('Total active incidents (all groups) = ' + total);\n\n// More efficient: GlideAggregate COUNT per group\nvar ga = new GlideAggregate('incident');\nga.addAggregate('COUNT');\nga.groupBy('assignment_group');\nga.addActiveQuery();\nga.query();\nwhile (ga.next()) {\n  var grp = ga.assignment_group;\n  var count = ga.getAggregate('COUNT');\n  gs.info('Group ' + grp + ' has ' + count + ' active incidents');\n}</pre><p>GlideRecord may be more appropriate if you not only need counts but also need to read or update individual incident fields in the same loop (for example, iterating records to set a flag), whereas GlideAggregate is preferred when you only need aggregated metrics for reporting or dashboards.</p>",
    "difficulty": "Senior",
    "company": "ServiceNow",
    "category": "Performance",
    "date": "Dec 16, 2025"
  },
  {
    "id": 3,
    "question": "Explain the execution order of Business Rules in ServiceNow, including the role of the 'Order' field, and describe scenarios for using Before, After, Display, and Async Business Rules.",
    "answer": "<h4>Key Concept</h4><p>Business Rules (BRs) in ServiceNow execute in a specific order based on their type (Before, After, Display, Async) and the 'Order' field, which determines sequence among BRs of the same type and timing. Lower order values execute first; e.g., Order 100 runs before Order 150.</p><h4>Execution Order</h4><p>The full server-side execution flow is:</p><ul><li><strong>Query Business Rules</strong> (on table query).</li><li><strong>Display Business Rules</strong> (after data read from DB, before form display).</li><li><strong>Before Business Rules</strong> (Order &lt;1000, then engines like Approval/Assignment, then Order &gt;=1000), before DB operation (insert/update/delete).</li><li><strong>DB Operation</strong>.</li><li><strong>After Business Rules</strong> (Order &lt;1000), then engines (e.g., Notifications, Workflow), then Order &gt;=1000 (active records only).</li><li><strong>Async Business Rules</strong> (queued in background after After BRs).</li></ul><pre>/* Example: Incident Before BR (Order 100) sets state */\n(function executeRule(current, previous) {\n    if (current.priority == 1) {\n        current.state = 6; // Resolved\n    }\n})(current, previous);\n\n/* After BR (Order 200) sends notification */\n(function executeRule(current, previous) {\n    gs.eventQueue('incident.resolved', current, current.number, current.priority);\n})(current, previous);</pre><h4>When to Use Each Type</h4><ul><li><strong>Before</strong>: Validate/set field values before DB save (e.g., auto-set state, prevent invalid data).</li><li><strong>After</strong>: Post-DB actions like update related records or trigger events (synchronous).</li><li><strong>Display</strong>: Modify form data before user sees it (e.g., compute display values).</li><li><strong>Async</strong>: Non-urgent, long-running tasks (e.g., external API calls) to avoid delaying user.</li></ul><p>Use low orders for foundational logic, higher for overrides. Test in sub-prod to avoid conflicts.</p>",
    "difficulty": "Senior",
    "company": "ServiceNow",
    "category": "Architecture",
    "date": "Dec 15, 2025"
  },
  {
    "id": 4,
    "question": "In ServiceNow, you need to calculate the total number of active incidents per assignment group to display in a dashboard. Explain which API you would choose between GlideRecord and GlideAggregate from a performance perspective, and why. Then rewrite the following GlideRecord-based code using GlideAggregate in a more efficient way: <pre>// example\nvar gr = new GlideRecord('incident');\ngr.addActiveQuery();\ngr.query();\nvar countsByGroup = {};\nwhile (gr.next()) {\n  var grp = gr.assignment_group.toString();\n  if (!countsByGroup[grp])\n    countsByGroup[grp] = 0;\n  countsByGroup[grp]++;\n}</pre>",
    "answer": "<h4>GlideRecord vs GlideAggregate for performance</h4>\n<p>For counting active incidents per assignment group, <strong>GlideAggregate</strong> is the better choice than <strong>GlideRecord</strong> because it performs the <em>COUNT/GROUP BY</em> directly in the database engine and returns only aggregated rows instead of every matching record. This reduces network traffic, memory usage, and script processing time, which is critical when the incident table is large or the query will be used in dashboards and scheduled jobs.</p>\n<p>Using GlideRecord for this problem forces the server to retrieve every active incident row, load all fields, and then loop in JavaScript just to compute counts. That design does not scale and will become slower as data volume grows, whereas GlideAggregate is explicitly designed and optimized for aggregation use cases (COUNT, SUM, AVG, MIN, MAX) and is more scalable for reporting-style queries.</p>\n\n<h4>Inefficient (but functionally correct) GlideRecord approach</h4>\n<pre>\n// Background Script – NOT optimal for large volumes\nvar gr = new GlideRecord('incident');\ngr.addActiveQuery();                     // only active incidents\ngr.query();\n\nvar countsByGroup = {};\nwhile (gr.next()) {\n  var grp = gr.getValue('assignment_group') || 'no_group';\n  if (!countsByGroup[grp])\n    countsByGroup[grp] = 0;\n  countsByGroup[grp]++;                  // counting in memory\n}\n\n// Example: log result\nfor (var g in countsByGroup) {\n  gs.info('Group ' + g + ' has ' + countsByGroup[g] + ' active incidents');\n}\n</pre>\n<p>This works, but it loads every active incident into memory and iterates over all of them just to compute counts, which is wasteful for reporting purposes.</p>\n\n<h4>Efficient GlideAggregate implementation</h4>\n<pre>\n// Background Script – optimized aggregation\nvar ga = new GlideAggregate('incident');\nga.addActiveQuery();                         // only active incidents\nga.groupBy('assignment_group');              // group at DB level\nga.addAggregate('COUNT');                    // COUNT(*) per group\nga.query();\n\nvar countsByGroup = {};\nwhile (ga.next()) {\n  var grp = ga.getValue('assignment_group') || 'no_group';\n  var count = parseInt(ga.getAggregate('COUNT'), 10);\n  countsByGroup[grp] = count;\n}\n\n// Example: log result for dashboard data source\nfor (var g in countsByGroup) {\n  gs.info('Group ' + g + ' has ' + countsByGroup[g] + ' active incidents (GA)');\n}\n</pre>\n<p>Here the database returns one row per assignment group with the precomputed count. The script never touches individual incident records, making it significantly more performant and better suited for dashboards and analytics.</p>",
    "difficulty": "Senior",
    "company": "ServiceNow",
    "category": "Performance",
    "date": "Dec 14, 2025"
  },
  {
    "id": 5,
    "question": "You are designing access controls for the Incident table. Explain, in detail, how ServiceNow evaluates multiple ACLs (table and field level) when a user attempts to update the `incident.short_description` via both the UI and a REST API, and describe at least three concrete security best practices you would apply to avoid data exposure or conflicting rules in this scenario.",
    "answer": "<h4>ACL Evaluation & Security Best Practices</h4>\n\n<p>When a user updates <strong>incident.short_description</strong> (via UI or REST), ServiceNow:</p>\n<ul>\n  <li>Identifies all matching <strong>record ACLs</strong> for operation <strong>write</strong>:\n    <ul>\n      <li>Table-level: <strong>incident</strong> and <strong>incident.*</strong></li>\n      <li>Field-level: <strong>incident.short_description</strong></li>\n    </ul>\n  </li>\n  <li>Requires that the user pass:\n    <ul>\n      <li>At least one matching <strong>table-level</strong> write ACL (no table write ACL may evaluate to true ⇒ update is denied).</li>\n      <li><strong>All</strong> matching <strong>field-level</strong> write ACLs on <strong>short_description</strong>.</li>\n    </ul>\n  </li>\n  <li>For each ACL, the user must:\n    <ul>\n      <li>Have one of the required roles (or list empty).</li>\n      <li>Match any “Data condition” or “Applies to” filter.</li>\n      <li>Have the script evaluate to true (if present).</li>\n    </ul>\n  </li>\n</ul>\n<p>This evaluation is identical for form, list, and REST API updates because ACLs are enforced server-side on every GlideRecord write.</p>\n\n<p><strong>Correct example – field-level write ACL script (incident.short_description)</strong></p>\n<pre>\n// Access Control: record | incident.short_description | write\n(function executeRule(current, previous /*null when async*/) {\n    var user = gs.getUser();\n    // Require itil role and same assignment group OR caller\n    var inItil = user.hasRole('itil');\n    var sameGroup = user.isMemberOf(current.assignment_group);\n    var isCaller = current.caller_id == user.getID();\n\n    // Only allow updates on non-closed incidents from same company\n    var isActive = current.state != 7; // 7 = Closed\n    var sameCompany = current.company == user.getCompanyID();\n\n    answer = inItil && (sameGroup || isCaller) && isActive && sameCompany;\n})();</pre>\n\n<p><strong>Correct example – table-level write ACL script (incident)</strong></p>\n<pre>\n// Access Control: record | incident | write\n(function executeRule(current, previous) {\n    var user = gs.getUser();\n    // Block updates to Major Incidents unless user has specific role\n    var isMajor = current.impact == 1 && current.urgency == 1;\n    var hasMajorRole = user.hasRole('major_incident_manager');\n\n    if (isMajor && !hasMajorRole) {\n        gs.warn('Blocked update to major incident ' + current.number);\n        answer = false;\n        return;\n    }\n    // Fallback: any itil user can write non-major incidents\n    answer = user.hasRole('itil');\n})();</pre>\n\n<p>Both scripts must evaluate to true for the update to succeed. A REST API integration that uses <code>GlideRecord.update()</code> or table API is subject to the same ACL checks; failing either ACL produces a 403 / security exception.</p>\n\n<p><strong>Incorrect / risky approach – over-broad wildcard ACL</strong></p>\n<pre>\n// BAD: record | incident.* | write\n(function executeRule(current, previous) {\n    // Grants write to ALL fields if user has itil, ignoring field sensitivity\n    if (gs.hasRole('itil'))\n        answer = true;\n\n    // No company or state checks, no field-specific logic\n})();</pre>\n\n<p>This wildcard ACL can unintentionally override stricter field ACLs and expose sensitive fields (e.g., <code>incident.u_pi_data</code>).</p>\n\n<p><strong>Security best practices in this scenario</strong></p>\n<ul>\n  <li><strong>Use explicit field ACLs for sensitive data</strong>: Avoid broad <strong>incident.*</strong> write ACLs; define specific rules for <strong>short_description</strong> and any PII/custom fields to prevent accidental exposure.</li>\n  <li><strong>Design for server-side enforcement first</strong>: Never rely on UI Policies or Client Scripts to protect fields; ensure all authorization logic is in record/field ACLs or server-side business rules so REST and background scripts are equally constrained.</li>\n  <li><strong>Minimize complexity and test for conflicts</strong>: Keep ACL scripts small, reusable (e.g., call a shared Script Include), and use “Debug Security Rules” plus impersonation of integration users and low-privilege roles to detect conflicts between table, wildcard, and field ACLs.</li>\n  <li><strong>Segment integration users</strong>: Give REST API users dedicated roles mapped to specific ACLs (e.g., only allow updating non-sensitive fields, or only incidents from a specific company) instead of reusing human roles like <strong>itil</strong>.</li>\n  <li><strong>Fail closed & log denied attempts</strong>: Prefer “deny unless” patterns, and log important denials in ACL scripts for auditability of suspicious update attempts through both UI and APIs.</li>\n</ul>",
    "difficulty": "Senior",
    "company": "ServiceNow",
    "category": "Security",
    "date": "Dec 14, 2025"
  },
  {
    "id": 6,
    "question": "In ServiceNow, you have several Business Rules on the incident table: one Display rule, two Before rules (orders 100 and 200), one After rule (order 100), and one Async rule (order 100). A user opens an existing incident, changes the priority, and clicks Update. Describe the execution order of these Business Rules during this lifecycle and explain a realistic use case for each type (Display, Before, After, Async), including why you would choose that type instead of the others for your use case.",
    "answer": "<h4>Business Rule Execution Order</h4><p>When a user opens and updates an incident, the Business Rules execute in this precise order:</p><ol><li><strong>Display Business Rule</strong> — Runs when record is loaded (query), before data is sent to client</li><li><strong>Before Business Rule (order 100)</strong> — Runs before database write</li><li><strong>Before Business Rule (order 200)</strong> — Runs after order 100, still before write</li><li><strong>Database Commit</strong> — Record is saved</li><li><strong>After Business Rule (order 100)</strong> — Runs immediately after commit</li><li><strong>Async Business Rule (order 100)</strong> — Queued for background execution</li></ol><h4>Use Case 1: Display Rule — Prepare UI Data</h4><p>Use Display rules to set scratchpad values for client scripts without modifying the record.</p><pre>// Display BR: Set scratchpad for client script\n(function executeRule(current, previous) {\n  var caller = current.caller_id.getRefRecord();\n  g_scratchpad.caller_vip = caller.vip.toString();\n  g_scratchpad.caller_department = caller.department.getDisplayValue();\n})(current, previous);</pre><p><strong>Why Display:</strong> Data is read-only, only needed for UI logic, and must not affect the record.</p><h4>Use Case 2: Before Rule — Validation & Auto-Population</h4><p>Use Before rules to validate data and auto-populate fields before the record is saved.</p><pre>// Before BR (order 100): Auto-set assignment group based on category\n(function executeRule(current, previous) {\n  if (current.category.changesTo('network')) {\n    current.assignment_group.setDisplayValue('Network Support');\n  }\n  \n  // Prevent invalid state transition\n  if (previous.state == 6 && current.state != 6) {\n    current.setAbortAction(true);\n    gs.addErrorMessage('Cannot reopen a resolved incident');\n  }\n})(current, previous);</pre><p><strong>Why Before:</strong> Validation must happen before commit; auto-population must be part of the same transaction.</p><h4>Use Case 3: After Rule — Create Related Records</h4><p>Use After rules when you need the saved record's sys_id or must ensure the parent record exists.</p><pre>// After BR (order 100): Create audit log after save\n(function executeRule(current, previous) {\n  if (current.priority.changesTo('1')) {\n    var audit = new GlideRecord('u_incident_audit');\n    audit.initialize();\n    audit.u_incident = current.sys_id;\n    audit.u_action = 'Priority escalated to Critical';\n    audit.u_changed_by = gs.getUserID();\n    audit.insert();\n  }\n})(current, previous);</pre><p><strong>Why After:</strong> The audit record references the incident sys_id, which must exist first.</p><h4>Use Case 4: Async Rule — External Integration</h4><p>Use Async rules for long-running operations that shouldn't block the user.</p><pre>// Async BR (order 100): Notify external system\n(function executeRule(current, previous) {\n  try {\n    var rm = new sn_ws.RESTMessageV2('External_Monitoring', 'POST');\n    rm.setRequestBody(JSON.stringify({\n      incident_number: current.number.toString(),\n      priority: current.priority.toString(),\n      short_description: current.short_description.toString()\n    }));\n    rm.execute();\n  } catch (e) {\n    gs.error('External notification failed: ' + e.message);\n  }\n})(current, previous);</pre><p><strong>Why Async:</strong> External API calls are slow and may fail; user shouldn't wait for them.</p><h4>Key Decision Framework</h4><ul><li><strong>Display:</strong> Read-only UI prep, no record changes</li><li><strong>Before:</strong> Validation, field population, abort actions</li><li><strong>After:</strong> Create/update related records, actions needing committed data</li><li><strong>Async:</strong> External calls, heavy processing, non-blocking operations</li></ul>",
    "difficulty": "Senior",
    "company": "ServiceNow",
    "category": "Architecture",
    "date": "Dec 14, 2025"
  },
  {
    "id": 7,
    "question": "Explain the ACL evaluation order in ServiceNow and describe a scenario where two conflicting ACLs exist for the same operation on the incident table (one denying write access for a specific role, another granting it). Which ACL wins, why, and what security best practices would you recommend to avoid such conflicts?<h4>Expected Answer Structure:</h4><ul><li>Describe ACL evaluation from most specific to most generic.</li><li>Explain short-circuit evaluation (first matching ACL that grants access wins).</li><li>Analyze the conflicting ACL scenario.</li><li>Provide best practices.</li></ul>",
    "answer": "<h4>ACL Evaluation Order</h4><p>ServiceNow evaluates ACLs from <strong>most specific to most generic</strong>: Table.none (row-level), Table.* (all fields), Table.field (specific fields). It checks roles first, then conditions, then script. Evaluation stops at the first ACL that grants access (short-circuit logic). If no ACL grants access, access is denied.</p><h4>Conflicting ACL Scenario</h4><p>For incident table write access: ACL1 (Table.incident Write, Role='itil') with condition denying access; ACL2 (Table.incident.* Write, Role='itil') granting access. <strong>ACL1 wins</strong> because Table.none is more specific than Table.* and evaluates first. User cannot write.</p><pre>/* Example ACL1 Script (Deny) */\nif (gs.getUser().hasRole('itil') &amp;&amp; current.state == 7) {\n    answer = false;  // Must use 'answer', not 'return'\n}</pre><h4>Security Best Practices</h4><ul><li><strong>Use Debug Security Rules</strong> to trace evaluation order and identify conflicts.</li><li>Avoid overlapping ACLs; prefer conditions/scripts over multiple rules.</li><li>Name ACLs consistently (e.g., 'incident_itil_write_restrict_state7').</li><li>Test with impersonation, not admin (admins bypass ACLs).</li><li>Conduct quarterly audits and document in ACL matrix.</li><li>Handle script errors (defaults to deny) and avoid complex scripts impacting performance.</li></ul><p>Following these ensures granular, maintainable security without unintended access grants.</p>",
    "difficulty": "Senior",
    "company": "ServiceNow",
    "category": "Security",
    "date": "Dec 13, 2025"
  },
  {
    "id": 8,
    "question": "You need to expose incident data to an external system and also trigger downstream automations in ServiceNow when new incidents are created. Describe how you would decide between using Integration Hub (Flow Designer with REST steps/spokes) and a custom Scripted REST API for this use case. In your answer, compare the two approaches in terms of governance, reusability, performance, error handling, and long‑term maintainability. Then, provide a concrete example showing how you would implement a simple \"get incident by number\" endpoint as a Scripted REST API, and explain how the same requirement could be implemented with Integration Hub (e.g., via a REST API Trigger + Flow) without writing server‑side script, including what trade‑offs that introduces.",
    "answer": "<h4>Key Concept</h4><p>Choosing between <strong>Integration Hub</strong> and a <strong>Scripted REST API</strong> depends on who will maintain the integration, how much custom logic is needed, and whether you want low-code governance via Flow Designer or full control via server-side JavaScript.</p><pre>// Scripted REST API example\n// Resource: GET /api/x_my_app/incidents/{number}\n(function process(request, response) {\n  var number = request.pathParams.number;\n  var gr = new GlideRecord('incident');\n  gr.addQuery('number', number);\n  gr.query();\n  if (gr.next()) {\n    var body = {\n      sys_id: gr.getUniqueValue(),\n      number: gr.getValue('number'),\n      short_description: gr.getValue('short_description'),\n      state: gr.getDisplayValue('state')\n    };\n    response.setStatus(200);\n    response.setBody(body);\n  } else {\n    response.setStatus(404);\n    response.setBody({message: 'Incident not found'});\n  }\n})(request, response);</pre><p>With Integration Hub, you could instead expose a Flow using a REST API Trigger, let the trigger accept an incident number as input, use a \"Lookup Record\" or \"Get Record\" action on the incident table, and return the fields as outputs, gaining visual governance and reusable actions at the cost of some low-level control and potential performance overhead compared to a tightly coded Scripted REST API.</p>",
    "difficulty": "Senior",
    "company": "ServiceNow",
    "category": "Integration",
    "date": "Dec 13, 2025"
  },
  {
    "id": 9,
    "question": "Explain the key differences between ServiceNow Integration Hub and Scripted REST APIs for building integrations. When would you choose one over the other? Provide a code example of a Scripted REST API endpoint that creates an incident from external data, and contrast it with how you'd achieve the same using Integration Hub.",
    "answer": "<h4>Key Concept</h4><p>Integration Hub is a no-code/low-code platform within Flow Designer that uses pre-built <strong>spokes</strong> and actions for scalable, maintainable integrations with external systems, consolidating automation in one interface. Scripted REST APIs offer custom, code-based endpoints for fine-grained control but require scripting expertise and higher maintenance.</p><h4>Differences</h4><ul><li><strong>Approach:</strong> Integration Hub: Visual flows, spokes (e.g., REST Step, Retry Policy), Flow Templates for rapid deployment without code. Scripted REST APIs: Custom JavaScript for request handling, full flexibility but complex error handling.</li><li><strong>Pros/Cons:</strong> Integration Hub excels in maintainability, scalability, out-of-box features like bi-directional sync, Connections Dashboard. Scripted REST: High customization for unique needs, but prone to maintenance overhead and lacks built-in retry/monitoring.</li><li><strong>Use Cases:</strong> Choose Integration Hub for standard integrations (e.g., ServiceNow-to-ServiceNow Incident sync) if licensed; use Scripted REST for highly specific logic not covered by spokes.</li></ul><h4>Scripted REST API Code Example</h4><p>Create a POST endpoint to insert an incident:</p><pre>// Scripted REST API Resource - POST /api/custom/incident\n(function process(/*RESTAPIRequest*/ request, /*RESTAPIResponse*/ response) {\n    var requestBody = request.body.data;\n    var gr = new GlideRecord('incident');\n    gr.initialize();\n    gr.short_description = requestBody.short_description;\n    gr.caller_id = requestBody.caller_id; // Resolve user\n    gr.insert();\n    response.setStatus(201);\n    response.setBody({sys_id: gr.getUniqueValue()});\n})(request, response);</pre><h4>Integration Hub Contrast</h4><p>In Integration Hub, use Flow Designer: REST API Trigger &gt; Data Pill mapping &gt; Create Record action on Incident table, with built-in error handling and monitoring. No scripting needed, scalable across instances.</p><p>This demonstrates Integration Hub's simplicity for common scenarios versus Scripted REST's power for bespoke requirements.</p>",
    "difficulty": "Senior",
    "company": "ServiceNow",
    "category": "Integration",
    "date": "Dec 13, 2025"
  },
  {
    "id": 10,
    "question": "In ServiceNow, explain Business Rule execution order, how Client Scripts differ from UI Policies (including execution sequence), and how you would design a scoped application to keep logic clean and maintainable.",
    "answer": "<p><b>Business Rule execution order</b></p><p>Business Rules execute server-side around database operations in this sequence, with multiple BRs of the same type ordered by <i>Order</i> value (lower runs first):</p><ul><li><b>Display</b> BR: Runs on query, before data reaches client (e.g., set default values on form load).</li><li><b>Before</b> BR: Before insert/update/delete; ideal for validation, field population, or <code>setAbortAction(true)</code>.</li><li><b>Database operation</b>: Insert/update/delete commits to DB.</li><li><b>After</b> BR: Post-commit; handles notifications, related record updates, integrations.</li><li><b>Async</b> BR: Queued after After BRs; for long-running tasks like external API calls.</li></ul><p>Engines (e.g., Data Policy, Workflow) interleave between Before/After phases based on order thresholds like &lt;1000 or &gt;=1000.</p>\n\n<p><b>Client Script vs UI Policy and execution sequence</b></p><ul><li><b>Client Scripts</b> run in-browser (onLoad/onChange/onSubmit/onCellEdit) for custom logic like GlideAjax calls or complex validations. They offer full control but require scripting.</li><li><b>UI Policies</b> declaratively control UI (mandatory, visible, readonly) with optional scripts; simpler for basic behaviors, no custom JS needed.</li></ul><p>Execution: Client Scripts & UI Policies fire first on user actions (e.g., field change), before submit reaches server. Client Scripts can override UI Policies via <code>g_form.setMandatory()</code> or event timing. Server then processes: Data Policies, Before BRs, DB, After/Async BRs.</p><p><strong>Guideline:</strong> Use UI Policies for simple UI rules; Client Scripts for dynamic logic; server BRs for data integrity (bypasses client).</p>\n\n<p><b>Scoped application design for clean, maintainable logic</b></p><p>For scoped apps, enforce separation: thin client layer, centralized server logic, respect scopes.</p><ul><li><b>Data model:</b> Custom tables extending task/sys_metadata; avoid global table pollution.</li><li><b>Centralize:</b> Script Includes as APIs; BRs call them (not inline logic).</li><li><b>Minimize BRs:</b> 1-2 per table calling helpers vs. many complex BRs.</li><li><b>Client:</b> UI Policies first; GlideAjax to Script Includes for server data.</li><li><b>Scope safety:</b> Use scoped APIs; request cross-scope privileges explicitly.</li><li><b>Best practices:</b> Prefix names (x_myapp_), version methods, unit test pure functions.</li></ul>\n\n<pre style=\"background-color: #f4f4f4; padding: 10px; border-radius: 4px;\">// Scoped Script Include: MyAppOrderUtil (centralizes validation logic)\nvar MyAppOrderUtil = Class.create();\nMyAppOrderUtil.prototype = {\n    initialize: function() {},\n\n    validateOrder: function(gr) {\n        if (!gr.u_customer.nil()) {\n            var cust = new GlideRecord('customer_account'); // Assume custom table\n            if (cust.get(gr.u_customer)) {\n                if (gr.u_amount < cust.getValue('u_credit_limit')) {\n                    gr.u_approved = true;\n                    gr.u_priority = (gr.u_urgency * gr.u_impact); // Calculate priority\n                    return true;\n                }\n            }\n        }\n        gs.addErrorMessage('Customer or credit limit invalid');\n        return false;\n    },\n\n    notifyRelated: function(orderSysId) {\n        var order = new GlideRecord('x_myapp_order'); // Scoped table\n        if (order.get(orderSysId)) {\n            gs.eventQueue('order.created', order, order.u_customer, ''); // Fire event\n        }\n    },\n\n    type: 'MyAppOrderUtil'\n};</pre>\n\n<pre style=\"background-color: #f4f4f4; padding: 10px; border-radius: 4px;\">// Before BR on x_myapp_order table (calls Script Include - CLEAN approach)\n(function executeRule(current, previous) {\n    var util = new MyAppOrderUtil();\n    if (!util.validateOrder(current)) {\n        current.setAbortAction(true); // Abort if invalid\n    }\n})(current, previous);</pre>\n\n<pre style=\"background-color: #f4f4f4; padding: 10px; border-radius: 4px;\">// After BR example (async notifications - avoids blocking)\n(function executeRule(current, previous) {\n    var util = new MyAppOrderUtil();\n    util.notifyRelated(current.getUniqueValue()); // Non-blocking event\n})(current, previous);</pre>\n\n<pre style=\"background-color: #f4f4f4; padding: 10px; border-radius: 4px;\">// Client Script onChange for u_customer (GlideAjax to Script Include - thin client)\nfunction onChange(control, oldValue, newValue, isLoading) {\n    if (isLoading || newValue == '') return;\n    var ga = new GlideAjax('MyAppOrderUtil'); // AJAX to scoped SI\n    ga.addParam('sysparm_name', 'getCustomerLimit');\n    ga.addParam('sysparm_customer', newValue);\n    ga.getXMLAnswer(function(answer) {\n        var limit = parseFloat(answer);\n        if (g_form.getValue('u_amount') > limit)\n            g_form.addErrorMessage('Amount exceeds customer limit: ' + limit);\n    });\n}</pre>\n\n<p>This design keeps logic reusable, testable, and scoped; avoids duplication across BRs/Scripts.</p>",
    "difficulty": "Senior",
    "company": "ServiceNow",
    "category": "Architecture",
    "date": "Dec 13, 2025"
  },
  {
    "id": 11,
    "question": "In a ServiceNow integration scenario, how would you decide between using Integration Hub (Flow Designer) and a Scripted REST API, how would you implement robust error handling in Flow Designer, and in which concrete cases is a MID Server mandatory? Provide example code.",
    "answer": "<p><b>Answer outline</b></p><ul><li>Compare Integration Hub vs Scripted REST API (when to use which).</li><li>Describe Flow Designer error handling patterns.</li><li>List key MID Server use cases.</li><li>Include sample Scripted REST API and RESTMessageV2 code.</li></ul><p><b>1. Integration Hub vs Scripted REST API</b></p><p>Use <b>Integration Hub / Flow Designer</b> when:</p><ul><li>You want low/no-code, faster delivery, and easier maintenance.</li><li>You can leverage pre-built spokes (e.g., REST, SOAP, Azure, Slack, etc.).</li><li>You need visual flows, centralized monitoring, and standard error handling.</li><li>Both parties have Integration Hub licenses and the pattern is standard CRUD/event-based integration between instances or SaaS apps.</li></ul><p>Use a <b>Scripted REST API</b> when:</p><ul><li>You expose a custom inbound API with complex validation, transformations, or non-standard behavior.</li><li>You must control request/response structure tightly (sync responses, custom HTTP codes, custom auth logic).</li><li>The integration logic is heavily script-centric and may grow beyond what is readable in Flow Designer.</li><li>You do NOT need Integration Hub transaction counting for inbound calls (only outbound calls via REST/HTTP count).</li></ul><p>Typical pattern in practice:</p><ul><li>Inbound: Scripted REST API or Flow REST Trigger (if simple, licensed).</li><li>Orchestration / business logic: Flow Designer / Subflows / Actions.</li><li>Outbound: Integration Hub spokes or scripted RESTMessageV2 when no spoke exists or you need full control.</li></ul><p><b>2. Flow Designer error handling</b></p><p>Key patterns:</p><ul><li>Use <b>Try/Catch</b> (Error handler) in Flow Designer: wrap risky steps (REST step, Script step, subflow) in a Try; on failure, branch to an Error handler path.</li><li>Configure <b>On Error</b> on individual actions: log to a custom log table, create an Incident, or send a notification.</li><li>Use <b>Flow variables</b> for status and error messages, pass them to Notification / Log steps.</li><li>In subflows, define <b>output values</b> such as success flag and error_message; calling flows can react based on those.</li><li>For REST / HTTP steps, check response status code and treat non-2xx as business errors even if the step does not technically fail.</li></ul><p><b>3. MID Server use cases</b></p><p>A MID Server is required when ServiceNow must reach resources that are not directly reachable over the public internet, typically:</p><ul><li>Discovery / Service Mapping in on‑prem or private networks.</li><li>Orchestration tasks (e.g., run PowerShell/SSH on servers, call on‑prem APIs, manage AD, VMware, etc.).</li><li>Integrations to internal HTTP(S)/database endpoints behind a firewall.</li><li>Credentialed scans, event collection agents, or any integration where network access is only possible from inside the customer network.</li></ul><p><b>4. Sample Scripted REST API and outbound REST with Integration Hub-style logic</b></p><p>Scripted REST Resource (inbound) that validates input, calls an outbound REST (could be an Integration Hub HTTP step conceptually), and handles errors:</p><pre>// Scripted REST Resource: POST /api/x_acme/ext/incidents\n(function process(/*RESTAPIRequest*/ request, /*RESTAPIResponse*/ response) {\n    var body = request.body.data;\n    if (!body || !body.short_description) {\n        response.setStatus(400);\n        response.setBody({\n            status: 'error',\n            message: 'short_description is required'\n        });\n        return;\n    }\n\n    try {\n        // Example: outbound call using RESTMessageV2 (could be replaced by Integration Hub HTTP action)\n        var r = new sn_ws.RESTMessageV2('x_acme_external_incident', 'create');\n        r.setStringParameterNoEscape('short_description', body.short_description);\n        r.setStringParameterNoEscape('priority', body.priority || '3');\n\n        var res = r.execute();\n        var httpStatus = res.getStatusCode();\n        var resBody = res.getBody();\n\n        if (httpStatus &gt;= 200 &amp;&amp; httpStatus &lt; 300) {\n            response.setStatus(201);\n            response.setBody({\n                status: 'success',\n                upstream_status: httpStatus,\n                upstream_body: resBody\n            });\n        } else {\n            gs.error('External incident creation failed. Status: ' + httpStatus + ', body: ' + resBody);\n            response.setStatus(502);\n            response.setBody({\n                status: 'error',\n                message: 'Failed to create incident in external system',\n                upstream_status: httpStatus\n            });\n        }\n    } catch (e) {\n        gs.error('Scripted REST exception: ' + e.message);\n        response.setStatus(500);\n        response.setBody({\n            status: 'error',\n            message: 'Unexpected server error'\n        });\n    }\n})(request, response);</pre><p>Example Flow Designer Script step (used inside a Try block) to centralize error propagation:</p><pre>// Flow Designer Script step\n(function executeStep(inputs, outputs) {\n    var gr = new GlideRecord('incident');\n    gr.initialize();\n    gr.short_description = inputs.short_description;\n    gr.caller_id = inputs.caller_id;\n\n    if (!gr.insert()) {\n        outputs.success = false;\n        outputs.error_message = 'Failed to insert incident record';\n        return;\n    }\n\n    outputs.success = true;\n    outputs.sys_id = gr.getUniqueValue();\n})(inputs, outputs);</pre>",
    "difficulty": "Senior",
    "company": "ServiceNow",
    "category": "Integration",
    "date": "Dec 13, 2025"
  },
  {
    "id": 12,
    "question": "You discover a business rule on the incident table that runs synchronously on insert/update and uses GlideRecord to compute real-time metrics and counts over millions of rows. The rule loops over records, does multiple aggregate-like calculations in memory, and occasionally times out in production. Walk through, in detail, how you would:\n\n1) Decide when to use GlideRecord vs GlideAggregate for this use case, including how each impacts row-level locking, heap usage, and query execution time on large tables.\n2) Redesign the logic to minimize round-trips and leverage database-side aggregation (e.g., COUNT, SUM, GROUP BY) instead of script-side loops.\n3) Optimize the query for the underlying MySQL index strategy and cardinality (e.g., choice of query fields, operators, and composite indexes), and explain what you would look for in the execution plan / Debug SQL output.\n4) Choose the correct script execution context (synchronous BR, async BR, Scheduled Script, Flow Designer action, or on-demand Script Include) to stay within ServiceNow governor limits (transaction duration, query counts, row limits) under peak load.\n5) Guard your solution against future data growth so that it remains performant at 10x the current record count.\n\nIllustrate your answer by refactoring the following inefficient code into a production-grade version, and explain every major change:\n\n<pre>// Original synchronous business rule\nvar gr = new GlideRecord('incident');\ngr.addQuery('active', true);\ngr.addQuery('priority', '1');\ngr.orderBy('sys_created_on');\ngr.query();\n\nvar count = 0;\nvar totalDuration = 0;\n\nwhile (gr.next()) {\n  if (gr.category == 'network' || gr.category == 'hardware') {\n    count++;\n    totalDuration += gs.dateDiff(gr.opened_at, gr.closed_at, true);\n  }\n}\n\ngs.info('High-priority infra incident count: ' + count);\ngs.info('Total duration (seconds): ' + totalDuration);</pre>\n\nRefactor this to a design that uses GlideAggregate where appropriate, minimizes data scanned, respects governor limits, and isolates heavy work from the synchronous transaction. Explain how your solution behaves differently when executed as a synchronous BR versus an async BR or scheduled job, and the trade-offs of each in terms of user experience and platform health.",
    "answer": "<p>A strong answer should:</p>\n<ul>\n<li>Explain why GlideAggregate is preferable for aggregation over large datasets (COUNT/SUM on the DB side) and when GlideRecord is still required for row-level operations.</li>\n<li>Discuss query selectivity, indexed fields, and how to minimize scanned rows and records loaded into memory.</li>\n<li>Mention transaction/gov limits (max time, query count, row limit) and why heavy logic should not run synchronously.</li>\n<li>Compare sync BR vs async BR vs Scheduled Script in terms of latency to user, retry behavior, and impact on node resources.</li>\n<li>Show awareness of future data growth (10x) and describe testing/monitoring strategies.</li>\n</ul>\n<p>One possible refactor:</p>\n<pre>// Script Include or reusable function\nvar IncidentMetrics = Class.create();\nIncidentMetrics.prototype = {\n  initialize: function() {},\n\n  getHighPriorityInfraMetrics: function() {\n    var result = {\n      count: 0,\n      totalDuration: 0\n    };\n\n    // Use GlideAggregate to keep aggregation in the DB\n    var ga = new GlideAggregate('incident');\n    ga.addQuery('active', true);                // assumes indexed/filtered\n    ga.addQuery('priority', '1');               // high cardinality if possible\n    ga.addQuery('category', 'IN', 'network,hardware');\n\n    // Pre-calc duration in a field (e.g., u_duration_secs) via BR/Flow,\n    // then aggregate that column instead of computing per row here\n    ga.addAggregate('COUNT');\n    ga.addAggregate('SUM', 'u_duration_secs');\n\n    ga.query();\n    if (ga.next()) {\n      result.count = parseInt(ga.getAggregate('COUNT'), 10) || 0;\n      result.totalDuration = parseInt(ga.getAggregate('SUM', 'u_duration_secs'), 10) || 0;\n    }\n\n    return result;\n  },\n\n  type: 'IncidentMetrics'\n};\n\n// Example: async business rule or scheduled job\n(function() {\n  var metrics = new IncidentMetrics().getHighPriorityInfraMetrics();\n  gs.info('High-priority infra incident count: ' + metrics.count);\n  gs.info('Total duration (seconds): ' + metrics.totalDuration);\n})();</pre>\n<p>Key points to explain:</p>\n<ul>\n<li>Duration is precomputed into an indexed numeric field (e.g., u_duration_secs) by a lightweight per-record rule; the expensive aggregation then runs with GlideAggregate in a separate async or scheduled context.</li>\n<li>category filter is pushed into the query instead of evaluated in the while loop, reducing records returned.</li>\n<li>No orderBy() is used because ordering is irrelevant for aggregation and would add sorting overhead.</li>\n<li>The aggregation is moved out of the synchronous user transaction (e.g., to an async BR or Scheduled Script), improving user response time and reducing risk of timeouts.</li>\n<li>Indexes should exist on (active, priority, category) or a composite index tailored to the common query pattern to keep scans small.</li>\n<li>For very large datasets, you might shard by date (e.g., sys_created_on &gt;= javascript:gs.daysAgo(30)) for “recent metrics,” or maintain a denormalized metrics table updated incrementally instead of full-scan aggregation.</li>\n<li>In a synchronous BR, you would only call this logic for truly necessary near-real-time metrics and ensure it cannot exceed governor limits (possibly by adding additional guards like setLimit, date windows, or feature flags).</li>\n</ul>",
    "difficulty": "Senior",
    "company": "ServiceNow",
    "category": "Performance",
    "date": "Dec 13, 2025"
  },
  {
    "id": 13,
    "question": "You are designing a scoped HR Case Management app that extends global task tables. The customer has these requirements:\n\n1) On case insert/update, multiple Business Rules must: (a) normalize data, (b) enforce validation, (c) trigger downstream integrations, and (d) write audit records. Some must run synchronously, some can be deferred.\n2) On the case form, certain fields must be mandatory/read-only/hidden based on complex conditions, but must also work in Workspace and mobile.\n3) The app must be fully deployable via Update Sets across dev/test/prod, minimizing collisions with global artifacts and allowing parallel feature streams.\n\nDescribe, in detail, how you would:\n\n• Design the Business Rule layer (including before/after/async usage and explicit execution order) so behavior is deterministic, easy to debug, and safe from recursion when extending global tables.\n• Decide when to use Client Scripts vs UI Policies vs Data Policies vs Server-side logic for the form behavior, given performance, maintainability, and cross‑UI compatibility.\n• Structure the scoped application (naming, table choices, cross-scope calls, Script Includes, and protection policies) to prevent unintended interaction with global Business Rules while still leveraging platform features.\n• Define an Update Set strategy that supports parallel development, conflict avoidance with global updates, and safe promotion/rollback for this app (include how you’d handle global artifacts you must touch).\n\nBe explicit about trade-offs and failure modes (e.g., race conditions between rules, client/server mismatches, or Update Set merge issues), and how your design mitigates them.",
    "answer": "<p>A strong answer should address each layer explicitly and show how they interact end‑to‑end.</p><p><b>1) Business Rule layer & execution order</b></p><ul><li>Use a <i>pipeline pattern</i> of BRs with clear responsibilities and explicit order values:\n<ul><li>Before BR (low order, e.g., 100): data normalization (defaults, derived fields).</li>\n<li>Before BR (higher order, e.g., 200): validation and abort logic (gs.addErrorMessage, setAbortAction).</li>\n<li>After BR (e.g., 300): updates to related records that must see committed data.</li>\n<li>Async BR (e.g., 400+): notifications, integrations, heavy processing.</li></ul></li>\n<li>On extended global tables, constrain BRs to the scoped app (table in scope, conditions, and script guards) and avoid changing global behavior.</li>\n<li>To avoid recursion:\n<ul><li>Never call current.update() in before/after BRs; use setWorkflow(false) or flags (current.u_skip_br = true) when a server-side update is needed.</li>\n<li>Centralize logic in Script Includes and have BRs call those, so order concerns are mostly orchestration, not duplicated logic.</li></ul></li>\n<li>Make execution deterministic:\n<ul><li>Define and document a standard order banding (0–99 platform/global, 100–199 normalization, 200–299 validation, etc.).</li>\n<li>Use conditions instead of large scripted if/else where possible for performance and clarity.</li></ul></li></ul><p><b>2) Client Script vs UI Policy vs server logic</b></p><ul><li>Use <b>UI Policies</b> for simple mandatory/read‑only/visible behavior, because they are declarative, easier to maintain, and can auto-generate reverse logic.</li>\n<li>Use <b>Client Scripts</b> only for complex client-side logic (e.g., dynamic choices, asynchronous lookups) that UI Policies cannot express cleanly.</li>\n<li>For requirements that must hold regardless of client (Workspace, mobile, imports, API), implement the <b>source of truth</b> on the server:\n<ul><li>Business Rules and Data Policies to enforce validation and defaults.</li>\n<li>Client/UI behavior should mirror server logic, not be the only enforcement.</li></ul></li>\n<li>For Workspace/mobile, favor patterns supported by those UIs (e.g., use Data Policies and BRs for mandatory/validation, and test that your UI Policies/Client Scripts are compatible or provide separate workspace UI actions).</li></ul><p><b>3) Scoped app design</b></p><ul><li>Create dedicated scoped tables (e.g., x_hr_case) that extend task only where needed. Avoid modifying global task BRs; instead, add scoped BRs with clear conditions (current.sys_class_name == 'x_hr_case').</li>\n<li>Encapsulate business logic in Script Includes (application scope, public APIs), and have BRs, Flow Designer actions, and integrations call those methods.</li>\n<li>Use cross-scope access only where necessary, with explicit Application Access settings and protection policies to avoid unwanted reuse or editing.</li>\n<li>Namespace artifacts (Script Includes, BRs, Client Scripts) with a consistent prefix (e.g., HR_) to reduce collisions and make Update Set review easier.</li></ul><p><b>4) Update Set strategy</b></p><ul><li>Use <b>feature-based Update Sets</b> in dev (one per story/feature) for the scoped app. Do not mix global changes and scoped changes in the same Update Set.</li>\n<li>For required global artifacts (e.g., a global BR you must adjust), isolate them into separate, clearly named Update Sets, and enforce review/approval.</li>\n<li>Baseline target instances first (commit global platform/Store updates), then apply app Update Sets in a controlled order: foundation/config sets first, then feature sets.</li>\n<li>Mitigate conflicts and merge issues by:\n<ul><li>Locking or coordinating ownership of shared artifacts.</li>\n<li>Using compare/preview in test and backing changes with XML export for critical items.</li></ul></li>\n<li>Define rollback strategies (back-out Update Sets where possible, or have compensating Update Sets / XML snapshots for high-risk changes).</li></ul><p><b>Failure modes & mitigations</b></p><ul><li><b>Race conditions / order issues</b>: overlapping BRs with ambiguous order; mitigated by banded order scheme and central Script Includes.</li>\n<li><b>Client/server mismatch</b>: client scripts allowing submissions the server rejects; mitigated by always enforcing rules server-side and aligning client behavior to server logic.</li>\n<li><b>Update Set collisions</b>: two teams editing same global BR; mitigated by ownership rules, separate Update Sets, and mandatory code reviews on global artifacts.</li></ul>",
    "difficulty": "Senior",
    "company": "ServiceNow",
    "category": "Architecture",
    "date": "Dec 13, 2025"
  },
  {
    "id": 14,
    "question": "You are designing a new, high‑volume bidirectional integration between ServiceNow and an external HR system. Walk through how you would decide between Integration Hub spokes, custom Scripted REST APIs, and Import Set + Transform Maps for the inbound side, and between Integration Hub REST steps, Outbound REST Messages, and Stream Connect vs Table API for the outbound side. Your answer must cover: (1) the decision criteria and trade‑offs for each pattern (governance, upgrade‑safety, throughput, and maintainability), (2) Flow Designer error‑handling and retry patterns you would apply (including when to use Retry Policy vs Flow Error Handler vs TRY + Go back to), (3) authentication choices for outbound REST (basic, OAuth 2.0, mutual TLS, and MID Server credentials) and when each is appropriate, (4) when a MID Server is mandatory or strongly recommended, and (5) specific performance optimization techniques and anti‑patterns for high‑volume traffic, including Stream Connect vs Table API.",
    "answer": "<h4>Key Concept</h4><p>An expert‑level ServiceNow integration design requires choosing the right pattern per use case, then hardening it with Flow Designer error handling, appropriate authentication, and performance controls. The core decision axis is <strong>data volume and latency requirements</strong> vs <strong>governance, upgrade safety, and maintainability</strong>.</p><h4>Pattern Selection</h4><ul><li><strong>Integration Hub + Flow Designer</strong>: Use when you want low‑code, governed integrations with reusable actions and spokes, native retry policies, and Flow Error Handler. Best for event‑driven, near real‑time interactions and when citizen developers must safely consume the integration. Avoid for extremely high throughput record ingestion where each record becomes a separate flow execution.</li><li><strong>Scripted REST API</strong>: Use when you need fine‑grained control over request/response shape, custom validation, bulk payloads, or custom security logic. Ideal for high‑volume batch ingestion (e.g., HR nightly sync) where you want to handle hundreds or thousands of records in one call, then process with Import Sets or direct table writes.</li><li><strong>Import Set + Transform Map</strong>: Use when the source sends flat/batch data, you need staging, mapping, coalescing, and transform logic. Prefer this for initial loads and large scheduled updates; decouple ingestion (Import Set table) from business logic (Transform Map + scripts).</li></ul><h4>Outbound: REST, Stream Connect, Table API</h4><ul><li><strong>Outbound REST Message / Integration Hub REST step</strong>: Use for request/response style APIs, moderate volume, and when you need built‑in authentication profiles (basic, OAuth 2.0, mTLS, API keys).</li><li><strong>Stream Connect</strong>: Use for <strong>very high volume, low‑latency, near real‑time</strong> streaming (Kafka, Kinesis, etc.). Offload downstream processing and avoid per‑record synchronous REST calls.</li><li><strong>Table API</strong>: Good for API clients reading/writing ServiceNow data, but avoid using it as an internal high‑volume polling mechanism from ServiceNow; prefer outbound pushes or Stream Connect.</li></ul><h4>Flow Designer Error Handling & Retry</h4><ul><li>Use a <strong>Retry Policy</strong> on REST steps for transient issues (timeouts, 429s, 5xx) with exponential backoff or honoring <code>Retry-After</code>.</li><li>Use <strong>Action Error Evaluation</strong> to classify HTTP 4xx/5xx into specific error codes and surface them as Action Status.</li><li>Use <strong>TRY + Go back to</strong> patterns when you must re‑run a sequence of steps, not just a single REST call.</li><li>Use the <strong>Flow Error Handler</strong> for terminal failures (after retries) to log, create incidents, or enqueue recovery subflows; don’t attempt to “resume” from the failing step.</li></ul><h4>Authentication & MID Server</h4><ul><li><strong>Basic Auth</strong>: Only with HTTPS and when the external API cannot support stronger methods; manage via Connection & Credential aliases.</li><li><strong>OAuth 2.0</strong>: Preferred for modern SaaS; use OAuth profiles so Flow/REST steps do not hard‑code tokens.</li><li><strong>Mutual TLS</strong>: Use for high‑security, internal, or regulated integrations that require client certificates.</li><li><strong>MID Server</strong> is mandatory or strongly recommended when the target is on‑prem, behind a firewall, needs network‑level connectivity not available from the public instance, or when you must isolate traffic for security/compliance.</li></ul><h4>Code Example (Scripted REST + Import Set)</h4><pre>/* Scripted REST Resource example for bulk HR user load */\n(function process(/*RESTAPIRequest*/ request, /*RESTAPIResponse*/ response) {\n  var body = request.body.data;\n  var users = JSON.parse(body).users || [];\n\n  var stager = new GlideImportSetRunner('u_hr_user_import', 'u_hr_user_staging');\n  var isGr = new GlideRecord('u_hr_user_staging');\n\n  users.forEach(function(u) {\n    isGr.initialize();\n    isGr.u_source_sys_id = u.id;\n    isGr.u_first_name = u.firstName;\n    isGr.u_last_name = u.lastName;\n    isGr.insert();\n  });\n\n  stager.setRunTransform(true);\n  stager.run();\n\n  response.setStatus(202);\n  response.setBody(JSON.stringify({received: users.length}));\n})(request, response);</pre><h4>Performance Considerations & Common Mistakes</h4><ul><li><strong>Do</strong> batch records (e.g., arrays of users) instead of one REST call per record; avoid Flow Designer patterns that trigger a separate flow run per row for massive loads.</li><li><strong>Do</strong> push instead of poll; avoid frequent polling of external systems with Table API or REST when webhooks, queues, or Stream Connect can push changes.</li><li><strong>Do not</strong> put heavy synchronous business rules on high‑volume integration tables; move logic to transforms, async jobs, or subflows.</li><li><strong>Do not</strong> use Flow Error Handler as a retry loop; use Retry Policies or TRY + Go back to, otherwise you risk infinite loops and unbounded executions.</li><li><strong>Do</strong> use audit and logging sparingly on staging tables; excessive logging can become the performance bottleneck.</li></ul>",
    "difficulty": "Senior",
    "company": "ServiceNow",
    "category": "Integration",
    "date": "Dec 13, 2025"
  }
]