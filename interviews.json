[
  {
    "id": 1,
    "question": "Explain the difference between GlideRecord and GlideAggregate. When would you use one over the other, and what are the performance implications?",
    "answer": "<p><strong>GlideRecord</strong> retrieves individual records and loads all fields into memory. <strong>GlideAggregate</strong> performs database-level aggregations (COUNT, SUM, AVG, MIN, MAX) without loading record data.</p><h4>Performance Implications:</h4><ul><li><strong>GlideRecord COUNT anti-pattern:</strong> <code>gr.query(); while(gr.next()) count++;</code> loads every record into memory - O(n) memory and time.</li><li><strong>GlideAggregate:</strong> <code>ga.addAggregate('COUNT'); ga.query();</code> executes a single SQL COUNT - O(1) memory.</li></ul><h4>Code Example:</h4><pre>// ❌ Bad - Loads 100K records into memory\nvar gr = new GlideRecord('incident');\ngr.addQuery('state', 1);\ngr.query();\nvar count = gr.getRowCount(); // Still bad!\n\n// ✅ Good - Single DB operation\nvar ga = new GlideAggregate('incident');\nga.addQuery('state', 1);\nga.addAggregate('COUNT');\nga.query();\nvar count = ga.next() ? ga.getAggregate('COUNT') : 0;</pre><h4>When to use GlideAggregate:</h4><ul><li>Counting records</li><li>Summing numeric fields (e.g., total cost)</li><li>Finding min/max values</li><li>GROUP BY operations with addGroupBy()</li></ul>",
    "difficulty": "Senior",
    "category": "Performance",
    "company": "ServiceNow",
    "date": "Dec 12, 2025"
  },
  {
    "id": 2,
    "question": "A Business Rule and a Client Script both update the same field. Describe the exact execution order and how to prevent race conditions.",
    "answer": "<p><strong>Execution Order (on form submit):</strong></p><ol><li><strong>Client Script (onSubmit)</strong> - Runs in browser BEFORE data sent to server</li><li><strong>Business Rule (before)</strong> - Runs on server BEFORE database write</li><li><strong>Database Write</strong> - Actual INSERT/UPDATE</li><li><strong>Business Rule (after)</strong> - Runs AFTER database write</li><li><strong>Async Business Rule</strong> - Queued, runs independently</li></ol><h4>Race Condition Scenario:</h4><pre>// Client Script sets priority to 1\ng_form.setValue('priority', '1');\n\n// Before BR also sets priority based on impact\ncurrent.priority = calculatePriority(current.impact);</pre><p>The Business Rule <strong>always wins</strong> because it runs after client script and directly modifies the GlideRecord before DB write.</p><h4>Prevention Strategies:</h4><ul><li><strong>Single source of truth:</strong> Let only BR handle field, use UI Policy for display</li><li><strong>Use g_scratchpad:</strong> Pass client decisions to server</li><li><strong>Check current.changes():</strong> In BR, respect user changes</li></ul><pre>// Business Rule - respect user's explicit change\nif (!current.priority.changes()) {\n  current.priority = calculatePriority(current.impact);\n}</pre>",
    "difficulty": "Senior",
    "category": "Architecture",
    "company": "Apple",
    "date": "Dec 12, 2025"
  },
  {
    "id": 3,
    "question": "How does ServiceNow's ACL evaluation work? Explain the order of evaluation and what happens when multiple ACLs match.",
    "answer": "<p><strong>ACL Evaluation Order (most specific to least):</strong></p><ol><li><strong>Table.Field</strong> - e.g., incident.caller_id (most specific)</li><li><strong>Table.*</strong> - e.g., incident.* (table wildcard)</li><li><strong>*.Field</strong> - e.g., *.caller_id (field across tables)</li><li><strong>*.*</strong> - Global default (least specific)</li></ol><h4>Multiple ACL Match Rules:</h4><p>When multiple ACLs match at the same specificity level, they are <strong>ANDed together</strong> - ALL must pass.</p><pre>// Two ACLs on incident.* read:\n// ACL 1: role = itil\n// ACL 2: script checks assignment_group\n// User must have itil AND pass the script</pre><h4>Critical Gotcha - The 'admin' Override:</h4><pre>// This ACL will NEVER block admin users!\n// Admin bypasses ACL evaluation entirely unless:\ngs.hasRole('admin') // Always true for admin\n\n// To restrict even admin, use:\nif (gs.hasRole('admin')) return false; // Explicit deny</pre><h4>Debug ACLs:</h4><pre>// Enable ACL debugging for troubleshooting\ngs.setProperty('glide.security.debug', 'true');\n// Check: System Logs > Security Debug</pre>",
    "difficulty": "Expert",
    "category": "Security",
    "company": "Google",
    "date": "Dec 11, 2025"
  },
  {
    "id": 4,
    "question": "Design a solution to process 50,000 records nightly without causing performance issues or hitting governor limits.",
    "answer": "<p><strong>Key Constraints:</strong></p><ul><li>Script execution timeout: 5 minutes (configurable)</li><li>Memory limits per transaction</li><li>Database connection limits</li></ul><h4>Solution Architecture:</h4><pre>// 1. Scheduled Job triggers Flow Designer\n// 2. Flow uses 'For Each' with batch processing\n// 3. Each batch runs as separate transaction\n\n// Scheduled Script:\nvar workflow = new GlideFlow('process_records_flow');\nworkflow.startAsync();</pre><h4>Flow Designer Approach:</h4><ol><li>Query records with <code>setLimit(500)</code> per batch</li><li>Use <strong>For Each</strong> action with parallel execution</li><li>Subflow handles individual record processing</li><li>Built-in retry and error handling</li></ol><h4>Alternative - Event-Driven:</h4><pre>// Create events in batches\nvar gr = new GlideRecord('large_table');\ngr.addQuery('processed', false);\ngr.setLimit(1000);\ngr.query();\nwhile (gr.next()) {\n  gs.eventQueue('custom.process.record', gr, gr.sys_id, '');\n  gr.processed = true;\n  gr.setWorkflow(false); // Skip BR\n  gr.autoSysFields(false); // Skip sys_updated\n  gr.update();\n}\n// Script Action processes each event async</pre><h4>Performance Tips:</h4><ul><li><code>setWorkflow(false)</code> - Skip Business Rules</li><li><code>autoSysFields(false)</code> - Skip audit fields</li><li><code>setLimit()</code> - Always batch</li><li>Use <code>updateMultiple()</code> for bulk updates</li></ul>",
    "difficulty": "Expert",
    "category": "Performance",
    "company": "ServiceNow",
    "date": "Dec 11, 2025"
  },
  {
    "id": 5,
    "question": "Explain ServiceNow's scoped application architecture. How do scope isolation and cross-scope access work?",
    "answer": "<p><strong>Scope Isolation Principles:</strong></p><ul><li>Each app runs in its own namespace (e.g., <code>x_myco_app</code>)</li><li>Tables, scripts, and records are scoped by default</li><li>Global scope can access everything; scoped apps are restricted</li></ul><h4>Cross-Scope Access Methods:</h4><pre>// 1. Script Include with 'Accessible from' = 'All scopes'\nvar si = new global.GlobalUtility();\n\n// 2. GlideScriptedProcessor for REST-like access\n// 3. Cross-scope API (sn_ws namespace)\n// 4. Application Property: glide.script.block.client.globals</pre><h4>Table Access Patterns:</h4><pre>// From scoped app accessing global table:\nvar gr = new GlideRecord('incident'); // Works - incident is global\n\n// From scoped app accessing another scope's table:\nvar gr = new GlideRecord('x_other_app_table');\n// ❌ Fails unless 'Application Access' granted</pre><h4>Granting Cross-Scope Access:</h4><ol><li>Navigate to target table → Application Access</li><li>Enable: Can read / Can create / Can update / Can delete</li><li>Or use sys_scope_privilege table</li></ol><h4>Security Best Practices:</h4><ul><li>Never put sensitive logic in 'Accessible from all scopes'</li><li>Use GlideScriptedProcessor as API gateway</li><li>Validate caller scope: <code>gs.getCurrentApplicationScope()</code></li></ul>",
    "difficulty": "Senior",
    "category": "Architecture",
    "company": "Apple",
    "date": "Dec 10, 2025"
  },
  {
    "id": 6,
    "question": "What is the 'current' object mutation problem in Business Rules, and how do you solve it?",
    "answer": "<p><strong>The Problem:</strong></p><p>In 'before' Business Rules, the <code>current</code> object is mutable and shared across all BRs in the same transaction. One BR's changes affect subsequent BRs.</p><pre>// BR 1 (Order: 100)\ncurrent.short_description = 'Modified by BR1';\n\n// BR 2 (Order: 200) - sees BR1's change!\nif (current.short_description.includes('BR1')) {\n  // This executes unexpectedly!\n}</pre><h4>Why It Happens:</h4><ul><li><code>current</code> is a reference to the same GlideRecord instance</li><li>All 'before' BRs share this reference</li><li>Changes are cumulative until DB write</li></ul><h4>Solutions:</h4><pre>// 1. Use previous object for original values\nif (previous.short_description != current.short_description) {\n  // Field actually changed by user\n}\n\n// 2. Use changes() method\nif (current.short_description.changes()) {\n  // User changed this field\n}\n\n// 3. Store original in scratchpad (first BR)\nif (!current.scratchpad) current.scratchpad = {};\ncurrent.scratchpad.original_desc = previous.short_description;\n\n// 4. Check operation type\nif (current.operation() == 'insert') {\n  // previous is empty on insert\n}</pre><h4>Best Practice:</h4><p>Always use <code>previous</code> object when comparing values, and <code>current.field.changes()</code> to detect user modifications vs BR modifications.</p>",
    "difficulty": "Senior",
    "category": "Scripting",
    "company": "Google",
    "date": "Dec 10, 2025"
  },
  {
    "id": 7,
    "question": "How would you implement a custom caching mechanism in ServiceNow to reduce database queries?",
    "answer": "<p><strong>ServiceNow Caching Options:</strong></p><ol><li><strong>GlideSystem Cache</strong> - <code>gs.getProperty()</code> with caching</li><li><strong>Script Include with Static Variables</strong> - Session-level</li><li><strong>GlideCacheManager</strong> - Enterprise cache API</li><li><strong>System Properties</strong> - Cached by platform</li></ol><h4>Implementation - Script Include Cache:</h4><pre>var CachedLookup = Class.create();\nCachedLookup.cache = {}; // Static - survives instance\nCachedLookup.cacheExpiry = {};\nCachedLookup.TTL = 300000; // 5 minutes\n\nCachedLookup.prototype = {\n  initialize: function() {},\n  \n  get: function(key) {\n    var now = new Date().getTime();\n    if (CachedLookup.cache[key] && \n        CachedLookup.cacheExpiry[key] > now) {\n      return CachedLookup.cache[key];\n    }\n    \n    // Cache miss - fetch from DB\n    var value = this._fetchFromDB(key);\n    CachedLookup.cache[key] = value;\n    CachedLookup.cacheExpiry[key] = now + CachedLookup.TTL;\n    return value;\n  },\n  \n  invalidate: function(key) {\n    delete CachedLookup.cache[key];\n  },\n  \n  _fetchFromDB: function(key) {\n    var gr = new GlideRecord('sys_properties');\n    gr.get('name', key);\n    return gr.value;\n  },\n  \n  type: 'CachedLookup'\n};</pre><h4>Caveats:</h4><ul><li>Cache is per-node in clustered environments</li><li>Use for read-heavy, rarely-changed data</li><li>Invalidate on relevant Business Rule updates</li></ul>",
    "difficulty": "Expert",
    "category": "Performance",
    "company": "ServiceNow",
    "date": "Dec 9, 2025"
  },
  {
    "id": 8,
    "question": "Explain the difference between synchronous and asynchronous Business Rules. What are the risks of async BRs?",
    "answer": "<p><strong>Synchronous Business Rules:</strong></p><ul><li>Execute in the same transaction as the triggering event</li><li>Block until complete</li><li>Can prevent record save (before) or access updated data (after)</li></ul><p><strong>Asynchronous Business Rules:</strong></p><ul><li>Queue as scheduled jobs</li><li>Execute in separate transaction</li><li>Cannot affect the original transaction</li></ul><h4>Critical Risks of Async BRs:</h4><pre>// RISK 1: Record may be modified/deleted before async runs\n// Async BR queued at 10:00:00\n// User deletes record at 10:00:01\n// Async runs at 10:00:05 - current is stale/gone!\n\n// Solution: Always re-fetch\nvar gr = new GlideRecord('incident');\nif (!gr.get(current.sys_id)) {\n  return; // Record deleted\n}</pre><pre>// RISK 2: No access to 'previous' object\n// previous is ALWAYS empty in async BR\nif (previous.state != current.state) {\n  // ❌ This never works in async!\n}</pre><pre>// RISK 3: Transaction isolation\n// Async cannot rollback original transaction\n// If async fails, original save still committed</pre><h4>When to Use Async:</h4><ul><li>Sending notifications</li><li>External integrations</li><li>Heavy processing that shouldn't block UI</li><li>Audit logging to external systems</li></ul>",
    "difficulty": "Senior",
    "category": "Architecture",
    "company": "Apple",
    "date": "Dec 9, 2025"
  }
]
